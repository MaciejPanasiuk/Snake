import { OnlyXBestPipe } from './only-xbest.pipe';

describe('OnlyXBestPipe', () => {
  it('create an instance', () => {
    const pipe = new OnlyXBestPipe();
    expect(pipe).toBeTruthy();
  });
});
